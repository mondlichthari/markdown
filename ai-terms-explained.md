# 一文读懂 AI 编程助手核心术语：MCP、Loop、Skills 等

这篇文章用通俗的语言，带你理解在使用 Cursor 这类 AI 编程助手时常见的几个关键概念。读完之后，你会明白它们各自是什么、解决什么问题、以及它们之间如何配合。

---

## 一、MCP（模型上下文协议）

**MCP** 全称 **Model Context Protocol（模型上下文协议）**，由 Anthropic 在 2024 年提出。

### 它是什么

你可以把 MCP 理解为 **"AI 世界的 USB-C 接口"**。在 MCP 出现之前，如果想让 AI 助手连接数据库、读取你的 Notion、调用 GitHub、查询天气……每一种连接都要单独定制，非常麻烦。MCP 制定了一套**统一的标准协议**，让 AI 模型可以用同一种方式去连接各种外部工具和数据源。

### 核心角色

- **MCP Server（服务端）**：对外提供能力的一方，比如一个"GitHub MCP 服务器"可以让 AI 创建 issue、读取代码。
- **MCP Client（客户端）**：使用这些能力的一方，比如 Cursor、Claude Desktop 等 AI 应用。
- **三类核心能力**：
  - **Tools（工具）**：AI 可以调用的动作，例如"发送邮件""查询数据库"。
  - **Resources（资源）**：AI 可以读取的数据，例如文件、API 返回内容。
  - **Prompts（提示模板）**：预设好的提示词模板，方便复用。

### 一句话总结

> MCP 让 AI 能够"插上各种插头"，安全、标准地连接现实世界的工具和数据。

---

## 二、Skills（技能）

**Skills** 是给 AI 助手"按需加载的专业说明书"。

### 它是什么

一个 Skill 本质上是一个文件夹，里面有一个 `SKILL.md` 文件，描述了"在什么情况下，应该怎么做某件事"。当你的请求和某个 Skill 的描述匹配时，AI 会**主动去读取这份说明书**，然后按照里面的步骤来完成任务。

### 为什么需要它

AI 模型不可能记住所有项目的特定规范和复杂流程。Skills 让你可以把**专业知识、操作步骤、最佳实践**封装起来，需要时才加载，既节省上下文，又保证执行的一致性。

### 举例

- 一个"创建 PR"的 Skill，会告诉 AI 你团队的分支命名规范、PR 描述模板。
- 一个"数据可视化"的 Skill，会规定用什么图表库、什么配色。

### 结构示例

```
my-skill/
└── SKILL.md      # 描述：何时触发、具体步骤、注意事项
```

### 一句话总结

> Skill 是"可复用的专业操作手册"，让 AI 在合适的时机自动按规范办事。

---

## 三、Loop（循环 / 定时任务）

**Loop** 指的是让一个任务**按固定或可变的时间间隔反复执行**。

### 它是什么

普通的对话是"你问一次，AI 答一次"。而 Loop 允许你设定：**"每隔 5 分钟，帮我执行一次某个操作"**。例如 `/loop 5m /检查测试` 就表示每 5 分钟跑一次测试检查。

### 典型用途

- **持续监控**：每隔几分钟检查 CI 是否通过、服务器是否正常。
- **轮询等待**：等待某个长任务完成后再继续。
- **周期性维护**:定时整理、汇总、巡检。

### 一句话总结

> Loop 把"一次性指令"变成"周期性自动执行"，适合需要反复盯着的任务。

---

## 四、Agent（智能体）

**Agent（智能体）** 是能够"自己思考、自己动手"完成多步骤任务的 AI。

### 它是什么

和只会回答问题的聊天机器人不同，Agent 能够：**理解目标 → 制定计划 → 调用工具 → 检查结果 → 修正并继续**，直到任务完成。它会自主决定下一步做什么，而不需要你手把手指挥每一步。

### 子智能体（Subagent）

复杂任务中，主 Agent 还可以派出多个**子智能体**并行干活，比如一个负责搜索代码、一个负责跑测试，最后汇总结果。

### 一句话总结

> Agent 是会自主规划和行动的 AI"员工"，能独立完成复杂任务。

---

## 五、Hooks（钩子）

**Hooks（钩子）** 是在 AI 执行过程的"特定时刻"自动触发的脚本。

### 它是什么

你可以设定：**"当 AI 每次要修改文件之前/之后，自动运行某段脚本"**。比如在每次编辑后自动格式化代码、自动运行检查、记录日志等。它让你能在 AI 的工作流程中插入自定义的自动化行为。

### 一句话总结

> Hook 是"在特定事件发生时自动执行的小程序",用来规范和增强 AI 的行为。

---

## 六、Rules（规则）

**Rules（规则）** 是你给 AI 设定的**持久化指令**，它会在每次对话中被遵守。

### 它是什么

比如你可以设一条规则："始终用简体中文回答""所有函数都要写类型注解""遵循我们团队的代码风格"。规则保存在项目里（如 `.cursor/rules/`），让 AI 长期记住你的偏好，而不用每次重复交代。

### 一句话总结

> Rule 是"长期生效的约定",让 AI 始终按你的偏好和规范工作。

---

## 七、概念之间的关系

把这些概念串起来，可以这样理解一个 AI 编程助手的工作生态：

| 术语 | 角色定位 | 一句话比喻 |
|------|---------|-----------|
| **Agent** | 执行任务的主体 | 会自己干活的"员工" |
| **MCP** | 连接外部世界的通道 | 万能"USB 接口" |
| **Skills** | 按需加载的专业知识 | "操作手册" |
| **Rules** | 长期遵守的约定 | "员工守则" |
| **Hooks** | 特定时刻的自动动作 | "自动化触发器" |
| **Loop** | 周期性重复执行 | "定时闹钟" |

它们共同构成了一套完整体系：**Agent 是核心执行者，MCP 帮它连接工具，Skills 教它专业做法，Rules 约束它的行为，Hooks 在关键节点自动干预，Loop 让任务能周期性运行。**

---

## 结语

理解这些术语，你就掌握了现代 AI 编程助手的"骨架"。它们的共同目标只有一个：让 AI 不再是一个孤立的聊天框，而是一个**能连接工具、懂你规范、可自动化、能自主完成复杂工作**的强大助手。下次再看到这些词，你就胸有成竹了。
